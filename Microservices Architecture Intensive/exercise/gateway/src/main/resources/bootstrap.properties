spring.application.name=${NAME}

spring.cloud.consul.host=${DISCOVERY_NAME}

spring.cloud.config.discovery.enabled=true
spring.cloud.config.discovery.serviceId=${CONFIG_NAME}
spring.cloud.config.fail-fast=true